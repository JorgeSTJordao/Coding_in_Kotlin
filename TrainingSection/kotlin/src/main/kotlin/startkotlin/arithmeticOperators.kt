package startkotlin

fun main(){
    //Operadores: +, -, *, /, %
    var price = 100

    //Em alguns casos podemos incrementar uma unidade depois de ter imprimido
    println(price++)

    //Em outros o valor é incrementado à variável para ser imprimido com ele já no momento que for impresso
    println(++price)
}